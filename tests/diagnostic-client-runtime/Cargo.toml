# 测试专属真实传输驱动，不部署。 / Test-only real transport driver, never deployed.
[package]
name = "status-diagnostic-client-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[target.'cfg(target_arch = "wasm32")'.dependencies]
diagnostic-client = { path = "../../crates/diagnostic-client" }
status-domain = { path = "../../crates/status-domain" }
worker = { version = "=0.8.5" }
serde_json = "1"
futures-util = "0.3"
wasm-bindgen = "=0.2.128"
