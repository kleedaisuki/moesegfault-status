[package]
name = "status-runtime-tests"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[target.'cfg(target_arch = "wasm32")'.dependencies]
status-backend = { path = "../../crates/status-backend" }
worker = { version = "=0.8.5", features = ["d1", "queue"] }
serde_json = "1"
