{
  "name": "@moesegfault/status",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build": "wrangler deploy --cwd ../.. --config wrangler.jsonc --dry-run --outdir workers/status/dist",
    "dev": "wrangler dev --cwd ../.. --config wrangler.jsonc"
  },
  "dependencies": {
    "@moesegfault/contracts": "workspace:*",
    "@moesegfault/domain-wasm": "workspace:*",
    "@moesegfault/telemetry": "workspace:*",
    "aws4fetch": "^1.0.20",
    "jose": "6.2.12",
    "zod": "^4.6.2"
  }
}
